[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "apex-money-gn"
version = "1.0.0"
description = "APEX Money Guinée - Flask API for Orange Money + MTN MoMo"
authors = [{name = "APEX Money"}]
requires-python = ">=3.8"
